import React from 'react';
import Common from './Common';
import bottom from '../../images/bottom.png';

const Bottom = () => {
  return (
    <div>
        <Common  imgsrc={bottom}  title="Create profiles for children." subtitle="Send children on adventures with their favourite characters in a space made just for them—free with your membership."/>
    </div>
  );
};

export default Bottom;